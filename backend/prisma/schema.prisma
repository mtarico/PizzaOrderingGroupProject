generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model MenuItem {
  id          Int     @id @default(autoincrement())
  category    String
  name        String
  description String
  price       Float
}
